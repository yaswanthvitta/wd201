<table>
    <tr>
        <th>Title</th>
        <th>Duedate</th>
    </tr>
    <% for(let i=0; i < allTodos.length; i++){ %>
        <tr>
            <td><%= allTodos[i].title %></td>
            <td><%= allTodos[i].dueDate %></td>
        </tr>
        <% } %>
</table>